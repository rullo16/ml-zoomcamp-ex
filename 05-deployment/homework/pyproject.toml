[project]
name = "homework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.141.1",
    "requests>=2.34.2",
    "scikit-learn==1.6.1",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
homework = "homework:main"

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
